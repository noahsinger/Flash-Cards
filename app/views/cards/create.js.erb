<% if @card.errors.any? %>
	// $("#alert").html( "<%= @card.errors.full_messages.join(', ') %>" );
<% else %>
	// $("#notice").html( "The \"<%= @card.content %>\" card was added" );
	$("#card_content").val( "" );
	$("#new_cards").prepend( "<%= j render( @card ) %>" );
	$('.rest-in-place').restInPlace( );
<% end %>